---
- hosts: all
  gather_facts: false
  tasks:
    - name: Set maas vlans
      maas_vlan:
        username: "{{ maas_username }}"
        password: "{{ maas_password }}"
        vlans: "{{ maas_configs['vlans'] }}"
        site: "{{ maas_site }}"
      delegate_to: localhost
